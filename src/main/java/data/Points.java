package data;

import net.dv8tion.jda.api.entities.Member;
import net.dv8tion.jda.api.events.message.guild.GuildMessageReceivedEvent;

public class Points {
    /*public static void addPoints(GuildMessageReceivedEvent e, int numPoints, Member member){
        Data.setUserPoints(member.getId(), Data.getUserPoints(member.getId()) + numPoints);
    }

    public static void addPoints(GuildMessageReceivedEvent e, int numPoints){
        addPoints(e, numPoints, e.getMember());
    }*/
}
